import { ReactNode } from 'react';
interface LayoutProps {
    children: ReactNode;
}
export declare const LayoutWrapper: ({ children }: LayoutProps) => import("react/jsx-runtime").JSX.Element;
export {};
